<div class="card">
   <%= image_tag port.thumb_image unless port.thumb_image.nil? %>
   <p class="card-text">
     <span><%= link_to port.title, portfolio_show_path(port) %></span> <%= port.subtitle %>
   </p>
 </div> 